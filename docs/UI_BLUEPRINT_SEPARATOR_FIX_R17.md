# UI Blueprint Separator Fix R17

This note documents the Editor CSS separator adjustment between the top menu and the Ribbon/Toolbar.

## Scope

- Adjust `.menu-bar` so it does not stretch across the full screen width.
- Remove the long right-side `border-bottom` from the menu bar.
- Keep the Ribbon as its own separated area with a controlled top border.
- Do not change button logic, commands, engine code, or database code.
