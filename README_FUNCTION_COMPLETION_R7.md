# EnergyLink Management - Function Completion Round 7

This package continues the Function Completion work and focuses on Setup.

Run:

```powershell
pnpm install
pnpm db:generate
pnpm dev:editor
```

Optional Engine:

```powershell
pnpm dev:engine
```

Setup works without login and does not create users or passwords.
