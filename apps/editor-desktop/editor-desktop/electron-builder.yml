appId: com.energylink.management.editor
productName: EnergyLink Editor
artifactName: EnergyLink-Editor-${version}-${os}-${arch}.${ext}
directories:
  output: release
files:
  - dist/**
  - dist-electron/**
  - package.json
extraMetadata:
  main: dist-electron/main.js
win:
  target:
    - target: dir
      arch:
        - x64
  executableName: EnergyLink Editor
asar: true
