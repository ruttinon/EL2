appId: com.energylink.management.monitor
productName: EnergyLink Monitor
artifactName: EnergyLink-Monitor-${version}-${os}-${arch}.${ext}
directories:
  output: release
files:
  - dist/**
  - dist-electron/**
  - package.json
extraMetadata:
  main: dist-electron/main.js
win:
  target:
    - target: dir
      arch:
        - x64
  executableName: EnergyLink Monitor
asar: true
